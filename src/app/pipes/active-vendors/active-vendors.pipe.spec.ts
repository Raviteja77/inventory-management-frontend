import { ActiveVendorsPipe } from './active-vendors.pipe';

describe('ActiveVendorsPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveVendorsPipe();
    expect(pipe).toBeTruthy();
  });
});
